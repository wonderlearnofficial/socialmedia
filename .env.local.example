VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_UPLOAD_SCRIPT_URL=
# Local-dev-only convenience: skip retyping the PIN in `npm run dev`. Never set
# this in a deployed environment — it's ignored in production builds anyway.
VITE_DEV_LOGIN_PIN=
